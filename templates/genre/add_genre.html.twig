{% extends 'adminDashboard.html.twig' %}
{% block body %}
<div class="body flex-grow-1 px-3">
        <div class="container-lg">
          <div class="car"></div>
            <div class="card mb-4">
                <div class="card-header"><strong>Thêm</strong><span class="small ms-1">Thể loại</span></div>
                    <div class="card-body">

                        {{ form_start(form, {'attr': {'class': 'row g-3', 'style': 'padding: 20px 20px'}}) }}

                            <div >
                                <label for="inputPassword4" class="form-label">Genrename</label>
                                {{ form_row(form.name) }}   
                            </div>

                                <div class="col-12">
                                    <button type="submit" class="btn btn-primary" style="background-color: green">Thêm thể loại</button>
                                </div>
                            </div>
                            
                        </form>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
</div>
{% endblock %}